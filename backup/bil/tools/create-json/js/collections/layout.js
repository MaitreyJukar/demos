(function (JSONCreator) {
    var Layout = JSONCreator.Collections.Base.extend({
        model: JSONCreator.Models.Layout,
    }, {

    });

    JSONCreator.Collections.Layout = Layout;
})(window.JSONCreator);