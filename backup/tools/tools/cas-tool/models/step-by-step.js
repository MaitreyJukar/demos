(function (MathUtilities) {
    'use strict';

    /**
    * A customized Backbone.Model that represents 'Step by Step' section in Cas Tool.
    * @class StepByStep
    * @constructor
    * @namespace Tools.CasTool.CasToolHolder.Models
    * @module CasTool
    * @submodule CasToolHolder
    * @extends Backbone.Model
    */
    MathUtilities.Tools.CasTool.CasToolHolder.Models.StepByStep = Backbone.Model.extend({
        /**
        * @property defaults
        * @type Object
        */
        defaults: {
        }

    }, {});
}(window.MathUtilities));