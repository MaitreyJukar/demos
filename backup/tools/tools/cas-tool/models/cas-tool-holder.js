(function (MathUtilities) {
    'use strict';

    /**
    * A customized Backbone.Model that represents Cas Tool.
    * @class CasTool
    * @constructor
    * @namespace Tools.CasTool.Models
    * @module CasTool
    * @extends Backbone.Model
    */
    MathUtilities.Tools.CasTool.CasToolHolder.Models.CasToolHolder = Backbone.Model.extend({
        /**
        * @property defaults
        * @type Object
        */
        defaults: {
        }

    }, {});
}(window.MathUtilities));