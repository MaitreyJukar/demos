(function (MathUtilities) {
    'use strict';

    /**
    * A customized Backbone.Model that represents 'Enter Equation' section in Cas Tool.
    * @class EnterEquation
    * @constructor
    * @namespace Tools.CasTool.CasToolHolder.Models
    * @module CasTool
    * @submodule CasToolHolder
    * @extends Backbone.Model
    */
    MathUtilities.Tools.CasTool.CasToolHolder.Models.EnterEquation = Backbone.Model.extend({
        /**
        * @property defaults
        * @type Object
        */
        defaults: {
        }

    }, {});
}(window.MathUtilities));